# ── faster-whisper (primary inference backend) ──
faster-whisper>=1.1.0

# ── HuggingFace Whisper (streaming / legacy) ──
transformers>=4.45.0,<5.0

# ── PyTorch ──
torch>=2.5.0
torchaudio>=2.5.0

# ── VAD ──
silero-vad>=5.1

# ── Server ──
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=12.0

# ── Audio processing ──
numpy>=1.26.0
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.11.0

# ── Flash Attention 2 (serve4.py — HF pipeline backend) ──
# Pre-built wheel for torch 2.10 + CUDA 12.8 + Python 3.12:
#   pip install "https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.9.0/flash_attn-2.8.3%2Bcu128torch2.10-cp312-cp312-linux_x86_64.whl"
# Or compile from source (slow, needs 15GB+ RAM):
#   MAX_JOBS=1 NVCC_THREADS=1 pip install flash-attn --no-build-isolation
flash-attn>=2.8.0

# ── Live captioning (live_caption.py) ──
bnunicodenormalizer>=0.1.0
loguru>=0.7.0
